安装软件的话, 如果默认目录在家目录, 请安装到/opt/软件名目录, 否则家目录太乱了; 如果默认目录不在家目录和其子目录, 就正常安装

我的远程主机用户名是 wangxiangyang, 我的本地主机用户名是 guga
每次你在后台做完测试请将后台清理干净
如果当前会话是再远程主机, 按照以下指令进行:

我的远程主机上面我没有 sudo 权限, 但是我有docker权限, 我现在有一个容器 ubuntu2404-full, 容器的用户名是 dev 你不要在远程主机外部对他进行任何修改, 你只能进入容器里面进行修改. 我的远程主机叫做 wangxiangyang, 它里面的硬件和主机不是隔离的, 因此可以做很多东西, 甚至可以在容器内运行图形化程序, 然后在远程主机的显示器上显示出来, 我经常需要做这个操作. 我在远程主机上进入容器的时候会运行:
xhost +local:
docker exec -it \
  --env DISPLAY="$DISPLAY" \
  ubuntu2404-full bash
因为只有运行这个它才可以在屏幕上显示图形化界面, 但是我在 ssh 连接的时候好像不能直接写 $DISPLAY, 因为我的ssh终端里的$DISPLAY的值是我的电脑的显示器的值而不是远程主机的显示器的值. 具体应该怎么解决看你的了.

你在远程主机的默认工作空间是在容器内, 我提到的路径或者其他的报错之类的也都默认在容器内
你每次运行指令之后记住把没有必要保留的测试后台都清理掉
/home/dev/company/ 是我的资料, 我现在开发的机械臂是sr5

如果当前会话是在本地主机, 请按照以下指令进行:

// todo
