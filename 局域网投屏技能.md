# TigerVNC 局域网投屏配置（自适应与自动清理版）

本文说明如何把一台 Linux 主机上的虚拟图形桌面投送到另一台 Linux 主机，并回传键盘、鼠标和滚轮输入。本文在基础配置之外，重点补充：

- GNOME Wayland/XWayland 与 HiDPI 环境下的尺寸自适应；
- 如何验证 Viewer、Xvnc 与图形应用的实际尺寸一致；
- RViz 等 Qt 应用的顶层窗口铺满方法；
- 多段演示切换时的重新校验；
- 演示结束后自动退出 Viewer、Xvnc 和应用进程。

文中的命令是运维模板。示例 IP、用户名、Display 编号和路径必须按实际环境替换。

## 1. 角色与配置变量

选择一台 Linux 主机作为服务端，另一台带显示器的 Linux 主机作为客户端。

| 变量 | 含义 | 示例值 |
|---|---|---|
| `server_ip` | 服务端局域网 IP | `10.0.0.10` |
| `client_ssh` | `客户端用户名@客户端地址` | `operator@10.0.0.20` |
| `display_number` | 虚拟 X11 Display 编号 | `101` |
| `vnc_port` | VNC TCP 端口 | `59101` |
| `display_root` | 安装与状态目录 | `/opt/tigervnc/remote-display` |
| `client_unit` | 客户端用户服务名称 | `remote-display-client` |

在服务端终端设置：

```bash
server_ip=10.0.0.10
client_ssh=operator@10.0.0.20
display_number=101
vnc_port=59101
display_root=/opt/tigervnc/remote-display
client_unit=remote-display-client
server_user="$(id -un)"
server_group="$(id -gn)"
```

数据方向：

```text
服务端                                               客户端

图形程序 -> 虚拟 X Display -> Xvnc TCP -> TigerVNC Viewer -> 显示器
                         <- 键盘/鼠标/滚轮 <-
```

## 2. 选择固定模式还是自适应模式

### 2.1 自适应模式（推荐用于投屏演示）

服务端允许客户端改变虚拟桌面尺寸：

```text
-AcceptSetDesktopSize=1
```

客户端启用最大化和远端动态调整：

```text
-Maximize=1
-RemoteResize=1
```

该模式适合 GNOME、Wayland/XWayland、HiDPI 和显示器缩放比例不为 100% 的环境。不能仅根据截图分辨率或显示器标称分辨率推算 X11 客户区，必须按第 6 节进行实测校验。

### 2.2 固定分辨率模式

服务端和客户端分别使用：

```text
-AcceptSetDesktopSize=0
-RemoteResize=0
```

该模式不会被客户端反向调整，但服务端 `-geometry` 必须与 Viewer 的真实客户区完全一致。若 Viewer 仍有标题栏、面板或 HiDPI 缩放，固定写成 `1920x1080` 很容易产生黑边或滚动条。

> 不要混用两种模式。例如服务端禁止调整、客户端却要求 `RemoteResize=1`，客户端无法真正完成自适应。

## 3. 安装软件

服务端：

```bash
sudo apt-get update
sudo apt-get install -y tigervnc-standalone-server tigervnc-tools xinput xdotool
```

确认使用 TigerVNC：

```bash
Xvnc -version
```

客户端：

```bash
ssh -t "$client_ssh" \
  'sudo apt-get update && sudo apt-get install -y tigervnc-viewer xdotool x11-utils'
```

## 4. 创建状态目录和认证文件

服务端：

```bash
sudo install -d -o "$server_user" -g "$server_group" -m 700 "$display_root"

vnc_secret="$(openssl rand -hex 4)"
printf '%s\n' "$vnc_secret" | tigervncpasswd -f > "$display_root/passwd"
unset vnc_secret
chmod 600 "$display_root/passwd"
```

VNC 认证只使用密码的前 8 个字符。不要把明文密码写进脚本或 Git。

客户端创建状态目录：

```bash
ssh -t "$client_ssh" "
  remote_user=\$(id -un)
  remote_group=\$(id -gn)
  sudo install -d -o \"\$remote_user\" -g \"\$remote_group\" -m 700 \
    '$display_root'
"
```

复制认证文件：

```bash
scp "$display_root/passwd" "${client_ssh}:${display_root}/passwd"
ssh "$client_ssh" "chmod 600 '$display_root/passwd'"
```

## 5. 启动自适应 Xvnc 服务端

在服务端执行：

```bash
setsid Xvnc ":${display_number}" \
  -geometry 1920x1080 \
  -depth 24 \
  -interface "$server_ip" \
  -rfbport "$vnc_port" \
  -SecurityTypes VncAuth \
  -PasswordFile "$display_root/passwd" \
  -FrameRate 60 \
  -CompareFB 2 \
  -AlwaysShared \
  -DisconnectClients=0 \
  -AcceptSetDesktopSize=1 \
  -localhost=0 \
  -desktop 'Remote Display' \
  -nolisten tcp \
  -noreset \
  > "$display_root/server.log" 2>&1 < /dev/null &

printf '%s\n' "$!" > "$display_root/server.pid"
```

说明：

- `-geometry` 是连接前的初始尺寸，不是自适应后的最终尺寸；
- `-AcceptSetDesktopSize=1` 是客户端调整桌面尺寸的必要条件；
- `-noreset` 让图形应用退出时 Xvnc 不自动结束，最终由清理流程统一停止；
- `-interface` 应只绑定可信局域网地址。

检查服务端：

```bash
ss -ltnp | grep "${server_ip}:${vnc_port}"
DISPLAY=":${display_number}" xdpyinfo >/dev/null \
  && echo "Display :${display_number} 正常"
tail -n 50 "$display_root/server.log"
```

## 6. 从 SSH 启动自适应 Viewer

### 6.1 导入客户端图形会话环境

客户端用户级 systemd 必须持有当前图形会话环境：

```bash
ssh "$client_ssh" \
  'systemctl --user show-environment | grep -E "^(DISPLAY|XAUTHORITY|WAYLAND_DISPLAY)="'
```

如果缺失，在客户端本地图形终端执行：

```bash
systemctl --user import-environment \
  DISPLAY XAUTHORITY WAYLAND_DISPLAY DBUS_SESSION_BUS_ADDRESS
```

GNOME 每次登录可能生成新的 `.mutter-Xwaylandauth.*` 文件。不要把旧的 `XAUTHORITY` 路径写死。

### 6.2 启动最大化 Viewer

```bash
ssh "$client_ssh" "
  systemctl --user stop '${client_unit}.service' 2>/dev/null || true
  systemctl --user reset-failed '${client_unit}.service' 2>/dev/null || true

  systemd-run --user \
    --unit='$client_unit' \
    --collect \
    --property=Restart=on-failure \
    --property=RestartSec=2 \
    /usr/bin/xtigervncviewer '${server_ip}::${vnc_port}' \
      -PasswordFile='$display_root/passwd' \
      -Maximize=1 \
      -RemoteResize=1 \
      -Shared=1 \
      -ViewOnly=0 \
      -PreferredEncoding=Tight \
      -AutoSelect=0 \
      -FullColor=1 \
      -CompressLevel=1 \
      -QualityLevel=8 \
      -PointerEventInterval=5 \
      -AcceptClipboard=1 \
      -SendClipboard=1 \
      -MenuKey=F8 \
      -ReconnectOnError=0
"
```

`主机::端口` 使用两个冒号；单冒号表示 VNC Display 编号。

### 6.3 为什么不把 `FullScreen=1` 当作唯一判据

在 GNOME Wayland/XWayland 环境中，TigerVNC 即使带有：

```text
-FullScreen=1
-FullScreenMode=Current
```

也可能只得到一个带标题栏的最大化窗口，`_NET_WM_STATE_FULLSCREEN` 并未实际出现。此时仅看启动命令会误判为全屏。

推荐使用 `-Maximize=1`，并等待窗口管理器明确设置：

```text
_NET_WM_STATE_MAXIMIZED_HORZ
_NET_WM_STATE_MAXIMIZED_VERT
```

如确实需要隐藏标题栏，可在客户端通过 TigerVNC 的 F8 菜单手动切换全屏；切换后仍应重新执行尺寸校验。

## 7. HiDPI 下的尺寸校验

### 7.1 不要直接使用截图像素

HiDPI/缩放环境中，截图尺寸、显示器逻辑尺寸和 X11 客户区尺寸可能不同。例如某次 150% 缩放实测中：

```text
截图约为 1920x1080
TigerVNC X11 客户区为 2880x1662
```

上述数值只是示例，不能硬编码到其他机器。

### 7.2 等待 Viewer 真正最大化后读取客户区

从服务端通过 SSH 查询客户端。SSH shell 不会自动继承 `XAUTHORITY`，需从用户级 systemd 环境读取：

```bash
viewer_size=$(ssh "$client_ssh" '
  export DISPLAY=:0
  export XAUTHORITY=$(systemctl --user show-environment |
    sed -n "s/^XAUTHORITY=//p")

  for i in $(seq 1 100); do
    wid=$(xdotool search --class "TigerVNC Viewer" 2>/dev/null | tail -1)
    if [ -n "$wid" ]; then
      state=$(xprop -id "$wid" _NET_WM_STATE 2>/dev/null)
      if printf "%s" "$state" | grep -q _NET_WM_STATE_MAXIMIZED_HORZ &&
         printf "%s" "$state" | grep -q _NET_WM_STATE_MAXIMIZED_VERT; then
        geometry=$(xdotool getwindowgeometry --shell "$wid")
        width=$(printf "%s\n" "$geometry" | sed -n "s/^WIDTH=//p")
        height=$(printf "%s\n" "$geometry" | sed -n "s/^HEIGHT=//p")
        printf "%sx%s\n" "$width" "$height"
        exit 0
      fi
    fi
    sleep 0.1
  done
  exit 1
')

printf 'Viewer 客户区：%s\n' "$viewer_size"
```

不要接受 Viewer 刚创建时的第一个非默认值。窗口可能短暂经历 `960x358` 等中间尺寸；必须等水平和垂直最大化状态都出现。

### 7.3 验证 Xvnc 桌面已经同步

```bash
desktop_size=$(DISPLAY=":${display_number}" xdpyinfo |
  awk '/dimensions:/{print $2; exit}')

printf 'Viewer=%s Xvnc=%s\n' "$viewer_size" "$desktop_size"

if [ "$viewer_size" != "$desktop_size" ]; then
  echo "尺寸尚未同步，不能启动图形演示" >&2
  exit 1
fi
```

只有 Viewer 客户区与 Xvnc 桌面完全一致，才算完成自适应。不能以“参数中写了 `RemoteResize=1`”代替运行时验证。

## 8. 启动图形应用并铺满虚拟桌面

图形程序必须连接虚拟 Display：

```bash
DISPLAY=":${display_number}" <图形程序启动命令>
```

Qt 应用可显式指定：

```bash
DISPLAY=":${display_number}" QT_QPA_PLATFORM=xcb <图形程序启动命令>
```

### 8.1 没有窗口管理器时，应用的“全屏”可能不生效

纯 Xvnc 桌面通常没有窗口管理器。Qt/RViz 的 `--fullscreen` 可能依赖窗口管理器，实际仍保持默认窗口尺寸。应在应用主窗口创建完成后，直接调整 X11 顶层窗口。

### 8.2 RViz 顶层窗口识别

RViz 会创建多个 class 同为 `rviz2` 的 X11 窗口，包括顶层主窗口和内部渲染子窗口。实测搜索结果中顶层窗口在前，因此使用 `head -1`：

```bash
top_window=$(DISPLAY=":${display_number}" \
  xdotool search --onlyvisible --class rviz2 2>/dev/null | head -1)
```

不要使用 `tail -1`；它可能选中内部渲染子窗口，导致子窗口尺寸变化但外层主窗口仍为默认的 `1200x900`，右侧继续出现黑边。

启动阶段还会出现 splash/小窗口，应等待主窗口宽度达到合理阈值：

```bash
for i in $(seq 1 200); do
  top_window=$(DISPLAY=":${display_number}" \
    xdotool search --onlyvisible --class rviz2 2>/dev/null | head -1)

  if [ -n "$top_window" ]; then
    current_width=$(DISPLAY=":${display_number}" \
      xdotool getwindowgeometry --shell "$top_window" 2>/dev/null |
      sed -n 's/^WIDTH=//p')

    if [ "${current_width:-0}" -ge 1000 ]; then
      break
    fi
  fi
  sleep 0.1
done
```

按 Xvnc 的当前尺寸铺满顶层窗口：

```bash
screen_size=$(DISPLAY=":${display_number}" xdpyinfo |
  awk '/dimensions:/{print $2; exit}')
screen_width=${screen_size%x*}
screen_height=${screen_size#*x}

DISPLAY=":${display_number}" \
  xdotool windowmove --sync "$top_window" 0 0
DISPLAY=":${display_number}" \
  xdotool windowsize --sync "$top_window" \
  "$screen_width" "$screen_height"
```

最后再次查询窗口尺寸，只有应用顶层窗口、Xvnc 和 Viewer 三者一致才算通过。

### 8.3 RViz 配置文件必须与启动入口匹配

- 单臂控制栈应使用该栈默认的 `robot_view.rviz`；
- 双臂 `arms.rviz` 依赖双臂 MarkerArray 发布器；
- 如果单臂 Demo 错用 `arms.rviz`，可能只显示网格而没有机械臂模型；
- 不要用“窗口已经显示”代替对模型、TF 和显示 Topic 的检查。

## 9. 多段演示和型号切换

不同应用或不同型号依次启动时，不能假设第一次校验结果永久有效。

已观察到的风险包括：

- 前一个 RViz 退出后，Xvnc 可能恢复到启动时的 `-geometry`；
- Viewer 最大化状态不变，但远端桌面尺寸可能重新变化；
- 新 RViz 会重新创建默认 `1200x900` 顶层窗口；
- 应用切换期间截图可能只看到空网格。

因此每一段演示开始前都应重复：

1. 校验 Viewer 已最大化；
2. 校验 Viewer 客户区尺寸等于 Xvnc 桌面尺寸；
3. 等待新应用主窗口创建；
4. 调整并校验应用顶层窗口尺寸；
5. 再开始动画或运动演示。

如果同一 VNC 会话内的尺寸会被重置，最稳妥的方式是每段演示独立启动并清理 Viewer/Xvnc。

## 10. 演示结束自动清理

一次性演示不应留下常驻 RViz、ROS 栈、Viewer 或 Xvnc。推荐在启动脚本中注册清理函数：

```bash
vnc_pid=""

cleanup_display() {
  ssh -o BatchMode=yes -o ConnectTimeout=5 "$client_ssh" \
    "systemctl --user stop '${client_unit}.service' 2>/dev/null || true; \
     systemctl --user reset-failed '${client_unit}.service' 2>/dev/null || true" \
    || true

  if [ -n "$vnc_pid" ] && kill -0 "$vnc_pid" 2>/dev/null; then
    kill -TERM "$vnc_pid" 2>/dev/null || true
    wait "$vnc_pid" 2>/dev/null || true
  fi
}

trap cleanup_display EXIT INT TERM
```

图形演示本身也应以前台进程运行。应用正常结束后，shell 退出并触发 `trap`；不要在演示结束后额外启动常驻监视栈。

清理后验证：

```bash
pgrep -af "Xvnc :${display_number}|rviz2" || true

ssh "$client_ssh" \
  "systemctl --user is-active '${client_unit}.service' 2>/dev/null || true; \
   pgrep -af xtigervncviewer || true"
```

期望 Viewer 服务为 `inactive`，并且没有目标 Xvnc/RViz 进程。

## 11. ROS 2 / RViz 补充注意事项

这些内容不是 TigerVNC 的通用要求，但在 ROS 2 演示脚本中很重要：

- 不要在 `source /opt/ros/<distro>/setup.bash` 前启用 `set -u`；部分 ROS 环境脚本会读取尚未定义的变量，例如 `AMENT_TRACE_SETUP_FILES`；
- 可以使用 `set -o pipefail` 保留管道中的 `ros2 launch` 退出码；
- 仿真和实机模式必须显式区分。无人值守时使用 `execute:=false`，不要仅凭 RViz 画面判断是否连接硬件；
- 退出时先让演示程序正常停止/禁用，再关闭 RViz、ROS 栈、Viewer 和 Xvnc；
- `No 3D sensor plugin(s) defined for octomap updates` 在未配置三维传感器的显示场景中可能只是已知提示，但仍应单独检查真正的 `FATAL`、进程异常退出和动作失败。

## 12. 验证鼠标和滚轮

服务端列出输入设备：

```bash
DISPLAY=":${display_number}" xinput list
```

应看到：

```text
TigerVNC pointer
TigerVNC keyboard
```

监听输入：

```bash
DISPLAY=":${display_number}" xinput test 'TigerVNC pointer'
```

正常的滚轮输出包含：

```text
button press   4
button release 4
button press   5
button release 5
```

按 `Ctrl+C` 结束监听。

## 13. 常见故障

### 13.1 客户端连接失败

服务端：

```bash
ss -ltnp | grep ":${vnc_port}"
ip address show | grep "$server_ip"
tail -n 100 "$display_root/server.log"
```

客户端网络：

```bash
ssh "$client_ssh" "nc -vz '$server_ip' '$vnc_port'"
```

### 13.2 SSH 中无法打开客户端 Display

```bash
ssh "$client_ssh" \
  'systemctl --user show-environment | grep -E "^(DISPLAY|XAUTHORITY|WAYLAND_DISPLAY)="'
```

交互式 SSH 不一定继承图形认证环境。执行 `xdotool`、`xprop` 或 `xwininfo` 前，应显式设置：

```bash
export DISPLAY=:0
export XAUTHORITY=$(systemctl --user show-environment |
  sed -n 's/^XAUTHORITY=//p')
```

### 13.3 右侧黑边或滚动条

依次检查：

1. Xvnc 是否使用 `AcceptSetDesktopSize=1`；
2. Viewer 是否使用 `Maximize=1` 和 `RemoteResize=1`；
3. Viewer 是否已经同时进入水平/垂直最大化状态；
4. Viewer 客户区是否等于 Xvnc 桌面；
5. 图形应用顶层窗口是否等于 Xvnc 桌面；
6. 是否误选了应用内部子窗口。

### 13.4 画面只有网格，没有模型

这通常不是 VNC 缩放问题。检查：

- RViz 配置文件是否匹配单臂/双臂入口；
- RobotModel 或 MarkerArray 的 Topic 是否存在；
- `robot_description`、TF 和 JointState 是否发布；
- 型号切换时是否使用了对应 SRDF/URDF。

### 13.5 画面延迟较大

优先保持 Tight 编码，调整：

```text
-CompressLevel=1
-QualityLevel=7
```

局域网带宽足够时，可把 `QualityLevel` 恢复为 `8` 或 `9`。提高压缩等级通常会增加两端 CPU 延迟。

## 14. 安全边界

`VncAuth` 只提供密码认证，不加密画面和输入。因此：

- 服务端必须用 `-interface "$server_ip"` 绑定可信内网地址；
- 不要把 VNC TCP 端口映射到公网；
- 不要使用 `-SecurityTypes None`；
- 跨越不可信网络时应使用 SSH 隧道或 TigerVNC TLS 安全类型；
- 密码文件权限保持 `600`，状态目录权限保持 `700`；
- 不要在日志、命令历史、文档或 Git 中保存明文密码。

## 15. 推荐启动顺序

自适应的一次性演示建议严格按以下顺序执行：

1. 注册 `trap` 清理函数；
2. 启动 `Xvnc`，允许 `SetDesktopSize`；
3. 启动 Viewer，要求最大化和 `RemoteResize`；
4. 等待最大化状态稳定；
5. 校验 Viewer 客户区与 Xvnc 桌面一致；
6. 启动图形应用；
7. 找到并调整应用顶层窗口；
8. 校验三者尺寸一致；
9. 开始演示；
10. 演示完成后正常停止应用；
11. 自动关闭 Viewer 和 Xvnc；
12. 检查无后台残留。

该顺序的核心原则是：**先验证显示链路，再开始演示；演示结束即清理，不依赖人工关闭窗口。**
