# TigerVNC 局域网投屏部署指南

本文给出一套通用的 Linux 局域网投屏方案：图形程序运行在服务端的虚拟 X11 桌面，客户端通过 TigerVNC Viewer 显示画面并回传键盘、鼠标和滚轮输入。

适用于远程演示、RViz/Qt 应用、无物理显示器的计算节点，以及 GNOME Wayland/XWayland 和 HiDPI 客户端。

## 1. 架构与部署参数

```text
服务端应用 -> 虚拟 X Display -> Xvnc -> 局域网 -> Viewer -> 客户端显示器
                                 <- 键盘 / 鼠标 / 滚轮 <-
```

- **服务端**：运行 Xvnc 和图形应用；
- **客户端**：连接显示器并运行 Viewer；
- **SSH 控制链路**：可选，用于从服务端自动启停客户端 Viewer。

部署前按实际环境设置：

```bash
server_ip=192.168.1.10
client_ssh=user@192.168.1.20
display_number=101
vnc_port=59101
display_root=/opt/tigervnc/remote-display
client_unit=remote-display-client
server_user="$(id -un)"
server_group="$(id -gn)"
```

示例 IP 和用户名必须替换。VNC 直连端口写作 `主机::端口`，其中是两个冒号。

## 2. 显示模式

推荐使用自适应模式：

```text
服务端：-AcceptSetDesktopSize=1
客户端：-Maximize=1 -RemoteResize=1
```

客户端最大化后，Viewer 会把真实客户区尺寸同步给 Xvnc，适合 HiDPI 和非 100% 缩放环境。

固定尺寸模式使用：

```text
服务端：-AcceptSetDesktopSize=0
客户端：-RemoteResize=0
```

固定模式下，服务端 `-geometry` 必须与 Viewer 客户区一致。不要混用两种模式。

## 3. 安装与初始化

Ubuntu/Debian 服务端：

```bash
sudo apt-get update
sudo apt-get install -y \
  tigervnc-standalone-server tigervnc-tools \
  xinput xdotool x11-utils
Xvnc -version
```

客户端：

```bash
sudo apt-get update
sudo apt-get install -y \
  tigervnc-viewer xdotool x11-utils wmctrl
```

`wmctrl` 用于在部分 GNOME Wayland/XWayland 环境中补发最大化请求。

### 3.1 创建独立状态目录

认证、PID 和日志统一放到 `/opt`，不读取或覆盖用户原有的 `~/.vnc` 配置。

服务端：

```bash
sudo install -d -o "$server_user" -g "$server_group" -m 700 "$display_root"
umask 077
openssl rand -hex 4 | tigervncpasswd -f > "$display_root/passwd"
chmod 600 "$display_root/passwd"
```

TigerVNC 传统认证只使用密码前 8 个字符。不要把明文密码写入脚本、日志或 Git。

客户端：

```bash
ssh -t "$client_ssh" "
  u=\$(id -un); g=\$(id -gn)
  sudo install -d -o \"\$u\" -g \"\$g\" -m 700 '$display_root'
"
scp "$display_root/passwd" "${client_ssh}:${display_root}/passwd"
ssh "$client_ssh" "chmod 600 '$display_root/passwd'"
```

可用 `sha256sum` 确认两端认证文件一致。

### 3.2 配置可选的 SSH 控制链路

自动化场景要求服务端能非交互 SSH 到客户端：

```bash
ssh -o BatchMode=yes -o ConnectTimeout=5 "$client_ssh" true
```

客户端 `authorized_keys` 中建议限制公钥来源和能力：

```text
from="服务端IP",restrict ssh-ed25519 <公钥> <注释>
```

## 4. 启动显示链路

### 4.1 服务端 Xvnc

```bash
setsid Xvnc ":${display_number}" \
  -geometry 1920x1080 \
  -depth 24 \
  -interface "$server_ip" \
  -rfbport "$vnc_port" \
  -SecurityTypes VncAuth \
  -PasswordFile "$display_root/passwd" \
  -FrameRate 60 \
  -CompareFB 2 \
  -AlwaysShared \
  -DisconnectClients=0 \
  -AcceptSetDesktopSize=1 \
  -localhost=0 \
  -desktop 'Remote Display' \
  -nolisten tcp \
  -noreset \
  > "$display_root/server.log" 2>&1 < /dev/null &

printf '%s\n' "$!" > "$display_root/server.pid"
```

`-geometry` 是连接前的初始值；自适应后的最终尺寸由 Viewer 决定。`-interface` 必须绑定可信内网地址。

检查服务端：

```bash
ss -ltnp | grep "${server_ip}:${vnc_port}"
DISPLAY=":${display_number}" xdpyinfo >/dev/null
tail -n 50 "$display_root/server.log"
```

### 4.2 客户端 Viewer

客户端图形会话先导入用户级 systemd 环境：

```bash
systemctl --user import-environment \
  DISPLAY XAUTHORITY WAYLAND_DISPLAY DBUS_SESSION_BUS_ADDRESS
```

不要写死 GNOME 动态生成的 `.mutter-Xwaylandauth.*` 路径。

从服务端启动 Viewer：

```bash
ssh "$client_ssh" "
  systemctl --user stop '${client_unit}.service' 2>/dev/null || true
  systemctl --user reset-failed '${client_unit}.service' 2>/dev/null || true

  systemd-run --user \
    --unit='$client_unit' --collect \
    --property=Restart=on-failure --property=RestartSec=2 \
    /usr/bin/xtigervncviewer '${server_ip}::${vnc_port}' \
      -PasswordFile='$display_root/passwd' \
      -Maximize=1 -RemoteResize=1 \
      -Shared=1 -ViewOnly=0 \
      -PreferredEncoding=Tight -AutoSelect=0 -FullColor=1 \
      -CompressLevel=1 -QualityLevel=8 \
      -PointerEventInterval=5 \
      -AcceptClipboard=1 -SendClipboard=1 \
      -MenuKey=F8 -ReconnectOnError=0
"
```

如果重连后未最大化，可在客户端补发：

```bash
wid=$(xdotool search --class 'TigerVNC Viewer' | tail -1)
wmctrl -i -r "$(printf '0x%x' "$wid")" \
  -b add,maximized_vert,maximized_horz
```

## 5. 运行时验证

不要用显示器标称分辨率、截图像素或启动参数代替运行时校验。HiDPI 下这些值可能不同。

验证顺序：

1. Viewer 窗口具有 `_NET_WM_STATE_MAXIMIZED_HORZ` 和 `_NET_WM_STATE_MAXIMIZED_VERT`；
2. 用 `xdotool getwindowgeometry --shell` 读取 Viewer 客户区；
3. 用 `xdpyinfo` 读取 Xvnc 桌面；
4. 两者必须完全一致。

```bash
# 客户端图形会话中
wid=$(xdotool search --class 'TigerVNC Viewer' | tail -1)
xprop -id "$wid" _NET_WM_STATE
xdotool getwindowgeometry --shell "$wid"

# 服务端
DISPLAY=":${display_number}" xdpyinfo |
  awk '/dimensions:/{print $2; exit}'
```

Viewer 最大化后，Xvnc 尺寸可能晚几个事件周期更新，应短暂轮询后再判定失败。切换应用或演示段落时需重新校验。

输入设备检查：

```bash
DISPLAY=":${display_number}" xinput list
DISPLAY=":${display_number}" xinput test 'TigerVNC pointer'
```

应看到 `TigerVNC pointer` 和 `TigerVNC keyboard`；滚轮通常对应 Button 4/5。

## 6. 运行图形应用

普通应用：

```bash
DISPLAY=":${display_number}" <图形程序> [参数...]
```

Qt/RViz：

```bash
DISPLAY=":${display_number}" QT_QPA_PLATFORM=xcb <图形程序> [参数...]
```

纯 Xvnc 桌面通常没有窗口管理器，应用自己的“全屏”参数可能无效。可直接调整顶层 X11 窗口：

```bash
window_class=rviz2
top_window=$(DISPLAY=":${display_number}" \
  xdotool search --onlyvisible --class "$window_class" 2>/dev/null |
  head -1)

screen_size=$(DISPLAY=":${display_number}" xdpyinfo |
  awk '/dimensions:/{print $2; exit}')
screen_width=${screen_size%x*}
screen_height=${screen_size#*x}

DISPLAY=":${display_number}" xdotool windowmove --sync "$top_window" 0 0
DISPLAY=":${display_number}" \
  xdotool windowsize --sync "$top_window" "$screen_width" "$screen_height"
```

RViz 可能创建多个 class 同为 `rviz2` 的窗口。应等待主窗口创建并选择顶层窗口，避免误选内部渲染子窗口或 splash。

## 7. 自动化脚本

本仓库的 `scripts/remote_display.sh` 封装了启停、最大化、尺寸同步、窗口铺满和自动清理。安装依赖、创建认证文件，并将脚本同步到两端的 `display_root` 后使用：

```bash
export REMOTE_DISPLAY_SERVER_IP=192.168.1.10
export REMOTE_DISPLAY_CLIENT_SSH=user@192.168.1.20
export REMOTE_DISPLAY_NUMBER=101
export REMOTE_DISPLAY_PORT=59101

scripts/remote_display.sh start
scripts/remote_display.sh status
scripts/remote_display.sh verify
scripts/remote_display.sh stop
```

一次性 RViz 会话：

```bash
scripts/remote_display.sh session --class rviz2 -- rviz2 [参数...]
```

应用退出、中断或失败后，脚本会自动关闭 Viewer 和 Xvnc。

## 8. 手动停止与清理

停止客户端：

```bash
ssh "$client_ssh" \
  "systemctl --user stop '${client_unit}.service' 2>/dev/null || true"
```

停止服务端时只使用保存的 PID，并确认它属于目标 Display：

```bash
server_pid=$(cat "$display_root/server.pid" 2>/dev/null || true)

if [[ "$server_pid" =~ ^[0-9]+$ ]] && kill -0 "$server_pid" 2>/dev/null; then
  cmdline=$(tr '\0' ' ' < "/proc/$server_pid/cmdline")
  if [[ "$cmdline" == *"Xvnc :${display_number}"* ]] ||
     [[ "$cmdline" == *"Xtigervnc :${display_number}"* ]]; then
    kill -TERM "$server_pid"
  fi
fi
unlink "$display_root/server.pid" 2>/dev/null || true
```

清理后期望 Viewer 为 `inactive`，且不存在目标 Xvnc 和图形应用进程。

## 9. 常见问题

### Viewer 无法连接

检查服务端监听地址、端口、日志和客户端到服务端的网络连通性：

```bash
ss -ltnp | grep ":${vnc_port}"
tail -n 100 "$display_root/server.log"
ssh "$client_ssh" "nc -vz '$server_ip' '$vnc_port'"
```

### SSH 中无法打开客户端桌面

确认客户端用户级 systemd 环境包含 `DISPLAY`、`XAUTHORITY`、`WAYLAND_DISPLAY` 和 `DBUS_SESSION_BUS_ADDRESS`，必要时在客户端本地图形终端重新导入。

### 黑边或滚动条

依次检查自适应参数、Viewer 双向最大化状态、Viewer/Xvnc 尺寸，以及应用顶层窗口尺寸。

### 延迟较高

保持 Tight 编码，可先把 `QualityLevel` 从 `8` 降到 `7`。不要优先提高压缩等级；更高压缩通常增加 CPU 延迟。

## 10. 安全与验收

传统 `VncAuth` 只认证、不加密画面和输入：

- 只绑定可信局域网地址，不映射到公网；
- 不使用 `SecurityTypes None`；
- 跨不可信网络时使用 SSH 隧道或 TigerVNC TLS；
- 状态目录保持 `700`，认证文件保持 `600`；
- 不在日志、文档、命令历史或 Git 中保存明文密码；
- 自动化 SSH 公钥限制来源和转发能力。

部署完成应同时满足：

- Xvnc 只监听指定内网地址和端口；
- 两端认证文件一致且权限正确；
- Viewer 客户区等于 Xvnc 桌面；
- 应用顶层窗口等于 Xvnc 桌面；
- 键盘、鼠标和滚轮输入正常；
- 会话结束后 Viewer、Xvnc 和测试进程全部清理。
